fastapi
uvicorn
qdrant-client
# sentence-transformers # Remove if ONLY using OpenAI
langchain
langchain-openai # New library for OpenAI integration
python-dotenv
tiktoken
unstructured # For document loading
markdown # For Markdown loading
# Add Notion loader if using Notion API directly
langchain-community # Often needed for loaders/splitters etc.
notion-client # Required by Notion loaders in LangChain